# 电子礼金簿功能增强实现计划

## 一、启动页功能实现

### 1. 创建启动页组件

* 创建 `src/components/SplashScreen.vue` 组件

* 设计启动页布局，包括应用 Logo、标题和功能按钮

* 实现启动页的基本样式和动画效果

### 2. 事务名称设置功能

* 在启动页添加事务名称输入框

* 实现事务名称的本地存储和读取

* 确保事务名称在应用中正确显示

### 3. 红白事模板选择

* 在启动页添加模板选择区域

* 实现红事（喜庆）和白事（肃穆）两种模板的切换

* 为不同模板设置对应的主题色和样式

### 4. 新建/选择原有数据功能

* 在启动页添加"新建礼金簿"和"选择原有数据"按钮

* 实现新建礼金簿时的初始化逻辑

* 实现选择原有数据时的文件浏览和加载逻辑

### 5. 启动页与主应用的衔接

* 修改 `src/App.vue`，添加启动页状态管理

* 实现启动页与主应用之间的平滑过渡

* 确保启动页设置的参数正确传递给主应用

## 二、低分辨率显示器适配

### 1. 响应式布局优化

* 分析当前布局在低分辨率下的问题

* 使用 Tailwind CSS 的响应式类进行布局调整

* 优化主内容区和侧边栏的布局方式

### 2. 字体和间距调整

* 为低分辨率设备设置合适的字体大小

* 调整组件间的间距，确保内容不拥挤

* 优化按钮和交互元素的大小，提高可点击性

### 3. 内容显示优化

* 实现表格和列表的横向滚动

## 三、技术实现细节

### 1. 状态管理

* 使用 Vue 3 的 Composition API 进行状态管理

* 实现启动页与主应用之间的数据传递

### 2. 本地存储

* 使用 localStorage 存储事务名称和模板选择

* 确保数据持久化和正确读取

### 3. 主题切换

* 基于现有的 CSS 变量系统实现主题切换

* 确保主题设置在应用中全局生效

### 4. 响应式设计

* 使用 Tailwind CSS 的响应式断点

* 针对不同屏幕尺寸优化布局

## 四、实施步骤

1. 创建启动页组件 `SplashScreen.vue`
2. 实现启动页的基本布局和样式
3. 添加事务名称输入和本地存储功能
4. 实现红白事模板选择功能
5. 添加新建/选择原有数据功能
6. 修改 `App.vue`，添加启动页状态管理
7. 实现启动页与主应用的过渡效果
8. 优化低分辨率显示器的布局
9. 测试所有功能的正确性
10. 进行性能和用户体验优化

