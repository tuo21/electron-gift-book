## 导入导出功能调整实施计划

### 阶段一：导出功能调整
1. 修改 `src/utils/export.ts`
   - `exportToExcel` 函数增加 `eventName` 参数
   - `exportToPDF` 函数增加 `eventName` 参数
   - 文件名格式改为：`{eventName}_${YYYYMMDD}`

2. 修改 `src/App.vue`
   - `handleExportExcel` 和 `handleExportPDF` 中传入 `appName.value`

### 阶段二：导入功能基础调整
1. 修改 `electron/main.ts`
   - 新增 `electron:parseImportFile` IPC 处理器
   - 读取 Excel 文件并返回表头和预览数据

2. 修改 `src/components/SplashScreen.vue`
   - 新增导入确认弹窗组件
   - 选择文件后先解析，显示事务名称编辑框
   - 确认后再创建数据库

### 阶段三：字段匹配功能
1. 新增 `src/utils/import.ts`
   - 定义标准字段别名映射表
   - 实现智能匹配算法
   - 实现字段映射对话框组件

2. 修改导入流程
   - 解析 Excel 后先进行字段匹配
   - 如果不完全匹配，弹出字段映射对话框
   - 用户确认映射关系后再执行导入

### 阶段四：数据验证
1. 在导入前增加数据预览功能
2. 标记异常数据（空值、格式错误等）
3. 显示导入统计信息

### 文件修改清单
- `src/utils/export.ts` - 修改导出文件名
- `src/App.vue` - 修改导出调用
- `electron/main.ts` - 新增导入解析IPC
- `src/components/SplashScreen.vue` - 新增导入确认弹窗
- `src/utils/import.ts` - 新增（字段匹配工具）
- `src/components/ImportFieldMapper.vue` - 新增（字段映射对话框组件）

请确认这个方案后，我将开始实施具体的代码修改。