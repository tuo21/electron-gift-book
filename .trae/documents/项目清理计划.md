# 项目清理计划

## 一、清理目标

移除与项目无关的临时文件、构建产物和开发环境缓存，整理历史遗留文档，保留一个干净的代码库。

---

## 二、需要删除的内容

### 2.1 构建产物（可通过构建命令重新生成）

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `electron-gift-book/dist/` | Vite前端构建输出 | 高 |
| `electron-gift-book/dist-electron/` | Electron主进程构建输出 | 高 |
| `electron-gift-book/build/` | Electron打包输出目录 | 高 |
| `electron-gift-book/src-tauri/target/` | Rust/Tauri编译缓存（非常大） | 高 |
| `win-unpacked/` | 根目录下的打包产物 | 高 |

### 2.2 IDE缓存文件

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `.vs/` | Visual Studio缓存（根目录） | 高 |
| `electron-gift-book/.vs/` | Visual Studio缓存（子目录） | 高 |

### 2.3 临时文件

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `localhost-1772262419375.log` | Electron调试日志文件 | 高 |
| `PDF位置信息.js` | 空文件，无内容 | 高 |
| `electron-gift-book/gift-book-fixed.exe` | 临时可执行文件 | 高 |
| `electron-gift-book/ai-tasks.json` | AI工具任务文件 | 中 |

### 2.4 重复/嵌套项目目录

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `electron-gift-book/electron-gift-book/` | 嵌套的旧项目副本，完全冗余 | 高 |

### 2.5 根目录冗余文件

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `src/` | 根目录下的空src目录（仅含空的main.ts） | 高 |
| `logo.png` | 可在electron-gift-book/public/images/logo.png找到 | 中 |

---

## 三、需要整理的文档

### 3.1 .trae/documents/ 计划文档（根目录）

共 **25个** 文档，建议处理方式：

**保留（重要参考）：**
- `电子礼金簿项目README文件.md` - 项目说明

**归档到 `docs/archive/` 目录：**
- 所有已完成的计划文档（Phase文档、功能实现计划等）

### 3.2 .trae/documents/ 计划文档（electron-gift-book目录）

共 **24个** 文档，与根目录重复或类似，建议：
- 删除与根目录重复的文档
- 归档已完成的计划文档

### 3.3 项目根目录文档

| 文件 | 处理方式 |
|------|----------|
| `README.md` | 保留，可整合内容 |
| `TAURI_MIGRATION_GUIDE.md` | 归档到 `docs/archive/` |

### 3.4 electron-gift-book 目录文档

| 文件 | 处理方式 |
|------|----------|
| `README.md` | 保留 |
| `调整说明文档.md` | 归档 |
| `项目介绍文档.md` | 整合到README |
| `PDF 模板测量指南.md` | 归档到 `docs/` |

---

## 四、执行步骤

### 步骤1：删除构建产物
```
删除以下目录：
- electron-gift-book/dist/
- electron-gift-book/dist-electron/
- electron-gift-book/build/
- electron-gift-book/src-tauri/target/
- win-unpacked/
```

### 步骤2：删除IDE缓存
```
删除以下目录：
- .vs/
- electron-gift-book/.vs/
```

### 步骤3：删除临时文件
```
删除以下文件：
- localhost-1772262419375.log
- PDF位置信息.js
- electron-gift-book/gift-book-fixed.exe
- electron-gift-book/ai-tasks.json
```

### 步骤4：删除嵌套项目副本
```
删除整个目录：
- electron-gift-book/electron-gift-book/
```

### 步骤5：删除根目录冗余文件
```
删除：
- src/ 目录
- logo.png（根目录）
```

### 步骤6：整理文档
```
1. 创建 docs/archive/ 目录
2. 移动已完成的计划文档到归档目录
3. 删除重复的计划文档
4. 整合项目说明文档
```

### 步骤7：更新 .gitignore
```
确保以下内容在 .gitignore 中：
- .vs/
- dist/
- dist-electron/
- build/
- target/
- *.log
```

---

## 五、预期效果

清理后的项目结构：
```
V1/
├── .trae/
│   └── documents/        # 仅保留必要的活跃计划
├── docs/
│   ├── archive/          # 归档的历史文档
│   └── PDF模板测量指南.md
├── electron-gift-book/
│   ├── .github/
│   ├── .vscode/
│   ├── Img/
│   ├── public/
│   ├── src/
│   ├── src-tauri/
│   ├── tests/
│   ├── package.json
│   ├── README.md
│   └── ...配置文件
└── README.md
```

---

## 六、注意事项

1. **备份重要数据**：执行删除前确认没有重要的未提交代码
2. **构建验证**：清理后运行 `npm run build` 验证项目可正常构建
3. **Git状态**：建议在清理后创建一个提交，便于回滚
